<proto>
  srcurl https://www.[NAME]graphics.org/releases/[NAME]-[AGE].[TARXZ]

<cluster>
  libpng pixman fontconfig glib2 xorg7-lib

<law>
  ./configure --prefix=/usr    \
            --disable-static \
            --enable-tee &&
make;
make install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

