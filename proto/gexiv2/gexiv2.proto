<proto>
  srcurl https://download.gnome.org/sources/[NAME]/0.12/[NAME]-[AGE].[TARXZ]

<cluster>
  exiv2 vala

<law>
  mkdir build &&
cd    build &&

meson --prefix=/usr .. &&
ninja;
ninja install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

