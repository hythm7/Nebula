<proto>
  source https://ftp.gnu.org/gnu/wget/wget-1.20.3.tar.gz

<law>


  ./configure                 \
    --prefix=/usr             \
    --sysconfdir=/etc         \
    --with-ssl=openssl        \
    --with-libssl-prefix=/usr;

  make -j [NPROC];

<install>
  make DESTDIR=[XYZ] install;

<desc>
  wget.
