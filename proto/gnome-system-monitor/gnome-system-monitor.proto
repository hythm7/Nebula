<proto>
  srcurl [GNOMEHTTP]/[NAME]/&gnome-minor-32;/[NAME]-[AGE].[TARXZ]

<cluster>
  adwaita-icon-theme gtkmm3 itstool libgtop librsvg

<law>
  mkdir build &&
cd    build &&

meson --prefix=/usr -Dsystemd=false .. &&
ninja;
mkdir build &&
cd    build &&

meson --prefix=/usr .. &&
ninja;
ninja install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

