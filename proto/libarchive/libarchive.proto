<proto>
  srcurl https://github.com/libarchive/libarchive/releases/download/v3.4.0/libarchive-3.4.0.tar.gz

<law>

  ./configure        \
    --prefix=/usr  \
    --without-xml2   \
    --without-nettle \
    --disable-static;

  make -j [NPROC];

<install>
  make DESTDIR=[XYZ] install;

<desc>
  libarchive.
