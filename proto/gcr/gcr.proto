<proto>
  srcurl [GNOMEHTTP]/[NAME]/&gnome-minor-28;/[NAME]-[AGE].[TARXZ]

<cluster>
  glib2 lib[NAME]ypt libtasn1 p11-kit gnupg2 gobject-introspection gtk3 libxslt vala

<law>
  sed -i -r 's:"(/desktop):"/org/gnome\1:' schema/*.xml &&

./configure --prefix=/usr     \
            --sysconfdir=/etc \
            --without-gtk-doc &&
make;
make install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

