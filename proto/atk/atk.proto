<proto>
  srcurl [GNOMEHTTP]/[NAME]/2.32/[NAME]-[AGE].[TARXZ]

<cluster>
  glib2 gobject-introspection

<law>
  mkdir build &&
cd    build &&

meson --prefix=/usr &&
ninja;
ninja install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

