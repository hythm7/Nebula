<proto>
  srcurl [SOURCEFORGE]/[NAME]/[NAME]-[AGE].[TARGZ]

<cluster>
  

<law>
  sed -i 's/env python/&3/' script/[NAME]*            &&
python3 setup.py install --prefix=/usr  \
                         --standard-lib \
                         --optimize=1   \
                         --install-data=/usr/share &&
rm -v /usr/bin/[NAME]*.bat

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

