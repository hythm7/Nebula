<proto>
  srcurl [GNOMEHTTP]/[NAME]/&gnome-minor-32;/[NAME]-[AGE].[TARXZ]

<cluster>
  gtk3 gobject-introspection startup-notification

<law>
  mkdir build &&
cd    build &&

meson --prefix=/usr -Dbuildtype=release .. &&
ninja;
ninja install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

