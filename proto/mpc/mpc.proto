<proto>
  srcurl https://ftp.gnu.org/gnu/mpc/mpc-1.1.0.tar.gz

<law>

  ./configure                        \
    --prefix=/usr                    \
    --disable-static                 \
    --docdir=/usr/share/doc/mpc-1.1.0;

  make -j [NPROC];

<install>
  make DESTDIR=[XYZ] install;

<desc>
  mpc.
