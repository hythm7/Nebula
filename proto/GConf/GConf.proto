<proto>
  srcurl [GNOMEHTTP]/[NAME]/3.2/[NAME]-[AGE].[TARXZ]

<cluster>
  dbus-glib libxml2 gobject-introspection gtk3 polkit

<law>
  ./configure --prefix=/usr \
            --sysconfdir=/etc \
            --disable-orbit \
            --disable-static &&
make;
make install &&
ln -s gconf.xml.defaults /etc/gconf/gconf.xml.system

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

