<proto>
  star gzip-1.10-x86_64-0-helix
  source http://ftp.gnu.org/gnu/gzip/gzip-1.10.tar.xz

<configure>

  ./configure --prefix=/usr;

<compile>
  make -j [NPROC];

<install>
  make DESTDIR=[XYZ] install;
  mkdir -pv [XYZ]/bin;
  mv -v [XYZ]/usr/bin/gzip [XYZ]/bin;

<desc>
  gzip.
