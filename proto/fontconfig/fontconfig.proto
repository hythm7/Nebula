<proto>
  srcurl https://www.freedesktop.org/software/[NAME]/release/[NAME]-[AGE].[TARBZ2]

<cluster>
  freetype2

<law>
  rm -f src/fcobjshash.h;
./configure --prefix=/usr        \
            --sysconfdir=/etc    \
            --localstatedir=/var \
            --disable-docs       \
            --docdir=/usr/share/doc/[NAME]-[AGE] &&
make;
make install;
install -v -dm755 \
        /usr/share/{man/man{1,3,5},doc/[NAME]-[AGE]/[NAME]-devel} &&
install -v -m644 fc-*/*.1         /usr/share/man/man1 &&
install -v -m644 doc/*.3          /usr/share/man/man3 &&
install -v -m644 doc/fonts-conf.5 /usr/share/man/man5 &&
install -v -m644 doc/[NAME]-devel/* \
                                  /usr/share/doc/[NAME]-[AGE]/[NAME]-devel &&
install -v -m644 doc/*.{pdf,sgml,txt,html} \
                                  /usr/share/doc/[NAME]-[AGE]

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

