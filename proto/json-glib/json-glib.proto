<proto>
  srcurl [GNOMEHTTP]/[NAME]/1.4/[NAME]-[AGE].[TARXZ]

<cluster>
  glib2

<law>
  mkdir build &&
cd    build &&

meson --prefix=/usr .. &&
ninja;
ninja install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

