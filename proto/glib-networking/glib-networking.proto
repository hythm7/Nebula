<proto>
  srcurl [GNOMEHTTP]/[NAME]/2.60/[NAME]-[AGE].[TARXZ]

<cluster>
  glib2 gnutls gsettings-desktop-schemas make-ca

<law>
  mkdir build &&
cd    build &&

meson --prefix=/usr          \
      -Dlibproxy=disabled .. &&
ninja;
ninja install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

