<proto>
  star gawk-5.0.0-x86_64-0-helix
  source http://ftp.gnu.org/gnu/gawk/gawk-5.0.0.tar.xz

<configure>

  sed -i 's/extras//' Makefile.in;
  ./configure --prefix=/usr;

<compile>
  make -j [NPROC];

<install>
  make DESTDIR=[XYZ] install;

<desc>
  gawk.
