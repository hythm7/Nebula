<proto>
  srcurl [GNOMEHTTP]/[NAME]/0.3/[NAME]-[AGE].[TARXZ]

<cluster>
  gtk3 lcms2 libarchive libjpeg libtiff libxslt

<law>
  mkdir build &&
cd    build &&

meson --prefix=/usr .. &&
ninja;
ninja install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

