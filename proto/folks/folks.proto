<proto>
  srcurl [GNOMEHTTP]/[NAME]/0.12/[NAME]-[AGE].[TARXZ]

<cluster>
  evolution-data-server gobject-introspection libgee python2 telepathy-glib bluez vala

<law>
  mkdir build &&
cd build &&

meson --prefix=/usr --sysconfdir=/etc .. &&
ninja;
ninja install

<install>
  make DESTDIR=[XYZ] install

<config>
  

<desc>
  [NAME].

